---
- name: Déploiement complet TaskFlow
  hosts: servers
  become: true
  vars:
    app_image: "ghcr.io/milhane41/taskflow"
    image_tag: "latest"
    project_dir: /opt/taskflow

  roles:
    - docker
    - app
    - monitoring
